# Example secrets file for Streamlit Cloud
# Copy this to secrets.toml locally or paste contents into Streamlit Cloud → App → Settings → Secrets

# OpenAI API (if you add LLM features later)
OPENAI_API_KEY = "set-in-streamlit-cloud"

# Supabase (if you add database features later)
SUPABASE_URL = "https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY = "set-in-streamlit-cloud"

# Add any other API keys or secrets needed by your app